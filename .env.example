# Copy to .env and fill in before running in production (docker compose reads it).

# Secret used to sign session cookies. REQUIRED in production — the app throws
# on start-up if it is missing. Generate a strong value with:
#   openssl rand -base64 32
SESSION_SECRET=
